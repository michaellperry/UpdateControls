<Window x:Class="UpdateControls.XAML.Test.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:u="clr-namespace:UpdateControls.XAML;assembly=UpdateControls.XAML"
    Title="{u:Update Title}" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="First Name:"/>
        <UserControl Grid.Row="0" Grid.Column="1" Padding="3">
            <TextBox Text="{u:Update FirstName}"/>
        </UserControl>
        <Label Grid.Row="1" Grid.Column="0" Content="Last Name:"/>
        <UserControl Grid.Row="1" Grid.Column="1" Padding="3">
            <TextBox Text="{u:Update LastName}"/>
        </UserControl>
        <Label Grid.Row="2" Grid.Column="0" Content="Display As:"/>
        <UserControl Grid.Row="2" Grid.Column="1" Padding="3">
            <ComboBox SelectedIndex="{u:Update DisplayStrategy}">
                <ComboBoxItem Content="{u:Update FirstLast}" />
                <ComboBoxItem Content="{u:Update LastFirst}"/>
            </ComboBox>
        </UserControl>
    </Grid>
</Window>
